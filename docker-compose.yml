services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: engineer-rag-qdrant
    ports:
      - "6333:6333"   # HTTP / dashboard
      - "6334:6334"   # gRPC
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_data:
